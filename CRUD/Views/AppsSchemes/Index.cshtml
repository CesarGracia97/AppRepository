@model IEnumerable<CRUD.Models.AppsScheme>

@{
    ViewData["Title"] = "Esquemas";
}

<h1>Esquemas</h1>

<p>
    <a asp-action="Create" class="btn btn-success">Crear Esquema</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DescriptionScheme)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NameScheme)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Id)
            </td>
                <td>
                @Html.DisplayFor(modelItem => item.DescriptionScheme)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NameScheme)
            </td>
            <td>
                <a asp-action="Edit" class="btn btn-warning" asp-route-id="@item.Id">Editar</a> |
                <a asp-action="Delete" class="btn btn-danger" asp-route-id="@item.Id">Eliminar</a>
            </td>
        </tr>
}
    </tbody>
</table>
